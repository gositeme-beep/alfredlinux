// Top-level build file — repos are in settings.gradle (dependencyResolutionManagement)
plugins {
    id 'com.android.application' version '8.2.2' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
