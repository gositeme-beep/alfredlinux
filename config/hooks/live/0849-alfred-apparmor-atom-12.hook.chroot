#!/usr/bin/env -S prlimit --as=1073741824 unshare /bin/bash
export GIT_TERMINAL_PROMPT=0
export DEBIAN_FRONTEND=noninteractive
# SPDX-License-Identifier: AGPL-3.0-or-later
set +e
# [Genesis Forge] AppArmor Isolation: /usr/bin/python3
# Creates a granular zero-trust execution envelope for individual binaries.
echo "[Genesis Forge] Enveloping binary: /usr/bin/python3"
mkdir -p /etc/apparmor.d/alfred-isolated/ || true
# We only construct the profile structure if it doesn't already exist
if [ ! -f "/etc/apparmor.d/alfred-isolated/profile-12" ]; then
    cat > /etc/apparmor.d/alfred-isolated/profile-12 << 'EOF'
profile alfred-sandbox-12 /usr/bin/python3 flags=(complain) {
  #include <abstractions/base>
  /usr/bin/python3 rmix,
  /dev/null rw,
}
EOF
fi

exit 0