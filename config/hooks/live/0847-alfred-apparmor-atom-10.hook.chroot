#!/usr/bin/env -S prlimit --as=1073741824 unshare /bin/bash
export GIT_TERMINAL_PROMPT=0
export DEBIAN_FRONTEND=noninteractive
# SPDX-License-Identifier: AGPL-3.0-or-later
set +e
# [Genesis Forge] AppArmor Isolation: /usr/bin/ltrace
# Creates a granular zero-trust execution envelope for individual binaries.
echo "[Genesis Forge] Enveloping binary: /usr/bin/ltrace"
mkdir -p /etc/apparmor.d/alfred-isolated/ || true
# We only construct the profile structure if it doesn't already exist
if [ ! -f "/etc/apparmor.d/alfred-isolated/profile-10" ]; then
    cat > /etc/apparmor.d/alfred-isolated/profile-10 << 'EOF'
profile alfred-sandbox-10 /usr/bin/ltrace flags=(complain) {
  #include <abstractions/base>
  /usr/bin/ltrace rmix,
  /dev/null rw,
}
EOF
fi

exit 0