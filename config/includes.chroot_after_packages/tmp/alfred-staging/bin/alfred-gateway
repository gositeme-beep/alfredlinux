#!/bin/sh
# /usr/bin/alfred-gateway — sovereign multi-channel inbox CLI
export OPENCLAW_HOME="${OPENCLAW_HOME:-$HOME/.alfred/gateway}"
export ALFRED_BRAND="Alfred"
exec /opt/alfred-gateway/openclaw.mjs "$@"
