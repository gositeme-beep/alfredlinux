server {
    listen 8080 default_server;
    listen [::]:8080 default_server;

    # Point to the user's public_html directory
    root /home/root/public_html;
    index index.html index.php;

    server_name _;

    location / {
        try_files $uri $uri/ =404;
    }
}
